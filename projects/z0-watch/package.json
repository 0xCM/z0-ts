{
  "name": "z0-watch",
  "version": "0.0.1",
  "description": "",
  "main": "dist/lib/main.js",
  "scripts": {
    "build": "tsc --build .",
    "buildv": "tsc --build . --verbose",
    "clean": "rm dist -rf",
    "clean-all": "rm dist -rf && rm node-modules -rf"
  },
  "author": "Chris Moore",
  "license": "MIT",
  "dependencies": {
    "@types/chokidar": "^1.7.5",
    "@types/node": "^10.7.0",
    "chokidar": "^2.0.4"
  }
}
